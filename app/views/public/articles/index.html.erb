<div class="container sm-12 md-8 lg-5 px-5 px-sm-0">

  <div class="row mt-5">
    <div class="leading-head mx-auto text-center">
      <h2>Leading</h2>
      <p>こちらではコーヒーに関する知識を深められる読み物をご用意しております。<br />
      　ごゆっくりご覧ください。
      </p>
    </div>
  </div>

  <div class="row mt-3">
    <div class="leading-body col-md-8 mx-auto">
      <table class="table">
        <thead>
          <tr class="table-active">
            <th>No.</th>
            <th>Title</th>
            <th>Category</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <% @articles.each do |article| %>
              <td><%= article.id %></td>
              <td><%= article.title %></td>
              <td><%= article.category.name %></td>
              <td>
                <%= link_to "詳細", article_path(article.id), class:"btn btn-primary px-3" %>
              </td>
          </tr>
            <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>